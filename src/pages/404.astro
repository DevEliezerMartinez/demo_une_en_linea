---
import Layout from "@/layouts/Layout.astro";
import Image_404 from "@/assets/imagenes/404.webp";
import { Image } from "astro:assets";
---

<Layout title="Página no encontrada">
  <section
    class="flex flex-col items-center justify-center min-h-screen px-4 text-center bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50"
  >
    <!-- Ilustración 404 -->
    <div class="mb-8 max-w-md w-full animate-float">
      <Image
        src={Image_404}
        alt="Ilustración 404 - Página no encontrada"
        class="w-full h-auto drop-shadow-2xl"
        loading="eager"
      />

      <!-- Mensaje principal -->
      <h1 class="text-3xl md:text-4xl font-bold mb-3 text-gray-800">
        ¡Ups! Página no encontrada
      </h1>

      <p class="text-base md:text-lg text-gray-600 mb-8 max-w-md">
        Parece que te has perdido. La página que buscas no existe o ha sido
        movida. Sentimos los incovenientes.
      </p>

      <!-- Botones de acción -->
      <div class="flex flex-col sm:flex-row gap-4">
        <a
          href="/"
          class="px-6 py-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-lg shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300"
        >
          🏠 Volver al inicio
        </a>

        <button
          onclick="history.back()"
          class="px-6 py-3 bg-white text-gray-700 font-semibold rounded-lg shadow-md hover:shadow-lg hover:scale-105 transition-all duration-300 border border-gray-200"
        >
          ← Regresar
        </button>
      </div>

      <!-- Enlaces útiles opcionales -->
      <div class="mt-12 pt-8 border-t border-gray-200 w-full max-w-md">
        <p class="text-sm text-gray-500 mb-4">
          ¿Necesitas ayuda? Prueba estos enlaces:
        </p>
        <div class="flex flex-wrap gap-4 justify-center text-sm">
          <a href="/" class="text-blue-600 hover:text-blue-800 transition">
            Inicio
          </a>
          <span class="text-gray-300">|</span>
          <a
            href="/contacto/solicitarinformacion"
            class="text-blue-600 hover:text-blue-800 transition"
          >
            Contacto
          </a>
          <span class="text-gray-300">|</span>
        </div>
      </div>
    </div>
  </section>

  <style>
    /* Animación de flotación suave */
    @keyframes float {
      0%,
      100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    .animate-float {
      animation: float 3s ease-in-out infinite;
    }

    /* Efecto hover en la imagen */
    .animate-float:hover {
      animation-play-state: paused;
      transform: scale(1.05);
      transition: transform 0.3s ease;
    }
  </style></Layout
>
