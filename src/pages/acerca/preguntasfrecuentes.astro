---
import Layout from "@/layouts/Layout.astro";
import { Image } from "astro:assets";
import Hero from "@/assets/Imagenes/heros/preguntas_frecuentes_desktop.webp";
import HeroMobile from "@/assets/Imagenes/heros/preguntas_frecuentes_mobile.webp";
import ContactSection from "@/components/sections/shared/GeneralCTA.astro";
import { Search, ChevronDown, MessageCircleQuestion } from "lucide-astro";
import ContactSidebar from "@/components/sections/home/ContactSidebar.astro";

const faqs = [
  {
    question: "¿Cuáles son los requisitos de ingreso?",
    answer:
      "Para el ingreso necesitas: certificado de bachillerato, acta de nacimiento, CURP, fotografías tamaño infantil.",
  },
  {
    question: "¿Puedo revalidar materias de otra universidad?",
    answer:
      "Sí, puedes revalidar materias de otras universidades. Debes presentar tu kardex oficial y los programas de las materias que deseas revalidar. Nuestro departamento académico evaluará la equivalencia.",
  },
  {
    question: "¿Las carreras están incorporadas a SICYT o UdeG?",
    answer:
      "Nuestras carreras están incorporadas al Sistema de Educación Superior de Jalisco (SICYT) y tienen validez oficial. Esto garantiza que tu título será reconocido a nivel nacional.",
  },
  {
    question: "¿Cómo puedo agendar una visita?",
    answer:
      "Puedes agendar una visita llamando a nuestro número telefónico, enviando un WhatsApp o rellenando el formulario de contacto. Te asignaremos un horario conveniente para ti.",
  },
  {
    question: "¿Ofrecen becas o apoyos económicos?",
    answer:
      "Sí, contamos con diferentes tipos de becas: por mérito académico, necesidad económica, y convenios con empresas. También ofrecemos planes de pago flexibles y descuentos por pronto pago.",
  },
  {
    question: "¿Cuáles son las fechas de inicio de clases?",
    answer:
      "Tenemos tres períodos de inicio: enero, mayo y agosto. Las fechas exactas varían cada año, pero generalmente inician la segunda semana de cada mes mencionado.",
  },
];
---

<Layout
  title="Preguntas Frecuentes UNE - Dudas sobre Carreras y Admisión"
  description="Respuestas a preguntas frecuentes sobre UNE: carreras, modalidades, costos, requisitos de admisión, becas y servicios estudiantiles."
>
  <main class="min-h-screen bg-gray-50">
    <!-- Hero Section -->
    <section
      class="relative min-h-[60vh] md:h-[600px] flex items-center justify-center overflow-hidden"
    >
      <!-- Background Images -->
      <Image
        src={Hero}
        alt="Fondo de preguntas frecuentes"
        class="absolute inset-0 w-full h-full object-cover hidden md:block"
        loading="eager"
      />
      <Image
        src={HeroMobile}
        alt="Fondo de preguntas frecuentes móvil"
        class="absolute inset-0 w-full h-full object-cover block md:hidden"
        loading="eager"
      />

      <!-- Hero Content -->
      <div class="relative z-10 container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="max-w-7xl mx-auto">
          <div
            class="flex flex-col md:flex-row items-center justify-between gap-8 md:gap-16"
          >
            <!-- Left side - Title -->
            <div class="flex items-center gap-6 text-white animate-fade-in-up">
              <div
                class="bg-white/10 backdrop-blur-md p-4 rounded-2xl border border-white/20 shadow-xl"
              >
                <MessageCircleQuestion
                  class="w-12 h-12 md:w-16 md:h-16 text-yellow-400"
                />
              </div>
              <h1
                class="text-5xl md:text-7xl font-bold tracking-tight text-white drop-shadow-md"
              >
                FAQ
              </h1>
            </div>

            <!-- Right side - Welcome Card -->
            <div class="w-full max-w-lg">
              <div
                class="bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-6 md:p-8 shadow-2xl animate-fade-in-up delay-100"
              >
                <h2 class="text-2xl font-bold text-white mb-3">
                  ¿Cómo podemos ayudarte?
                </h2>
                <p class="text-blue-50 text-base leading-relaxed">
                  Encuentra respuestas rápidas a las dudas más comunes. Si
                  necesitas más información, nuestro equipo está listo para
                  atenderte personalmente.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ List Section -->
    <section class="py-20 -mt-20 relative z-20 px-4">
      <div class="container mx-auto max-w-4xl">
        <div class="bg-white rounded-3xl shadow-xl p-6 md:p-12">
          <div class="text-center mb-10">
            <h2 class="text-3xl font-bold text-gray-900 mb-2">
              Preguntas frecuentes
            </h2>
            <p class="text-gray-500">
              Todo lo que necesitas saber para unirte a UNE
            </p>
          </div>

          <div class="grid gap-4">
            {
              faqs.map((faq, index) => (
                <div class="faq-item group border border-gray-100 rounded-xl overflow-hidden hover:border-blue-200 transition-all duration-300">
                  <button class="w-full flex items-center justify-between p-5 md:p-6 text-left bg-white hover:bg-gray-50 transition-colors focus:outline-none cursor-pointer">
                    <span class="font-semibold text-lg text-gray-800 group-hover:text-blue-700 transition-colors pr-8">
                      {faq.question}
                    </span>
                    <div class="shrink-0 text-blue-500 bg-blue-50 p-2 rounded-full group-hover:bg-blue-100 transition-colors">
                      <ChevronDown class="w-5 h-5 transition-transform duration-300 group-[.active]:rotate-180" />
                    </div>
                  </button>
                  <div class="faq-answer grid grid-rows-[0fr] transition-[grid-template-rows] duration-300 ease-out">
                    <div class="overflow-hidden">
                      <div class="p-6 pt-0 text-gray-600 leading-relaxed bg-white border-t border-transparent group-[.active]:border-gray-100">
                        {faq.answer}
                      </div>
                    </div>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <ContactSection backgroundImage={true} />
    <ContactSidebar />
  </main>

  <style>
    .faq-item.active .faq-answer {
      grid-template-rows: 1fr;
    }

    /* Animation classes */
    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
      opacity: 0;
      transform: translateY(20px);
    }

    .delay-100 {
      animation-delay: 0.1s;
    }

    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>

  <script>
    function setupFAQ() {
      const faqItems = document.querySelectorAll(".faq-item");

      faqItems.forEach((item) => {
        const button = item.querySelector("button");

        if (button) {
          button.addEventListener("click", () => {
            const isActive = item.classList.contains("active");

            // Close all other FAQ items
            faqItems.forEach((otherItem) => {
              if (otherItem !== item) {
                otherItem.classList.remove("active");
                // Update aria-expanded for other items if we were using it
              }
            });

            // Toggle current item
            item.classList.toggle("active");
          });
        }
      });
    }

    // Support both standard loads and view transitions
    document.addEventListener("DOMContentLoaded", setupFAQ);
    document.addEventListener("astro:after-swap", setupFAQ);
  </script>
</Layout>

<!-- Contact Section -->
