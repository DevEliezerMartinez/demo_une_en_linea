---
import Layout from "@/layouts/Layout.astro";
import Hero from "@/assets/Imagenes/Heros/Licenciaturas_gral.webp";
import Hero_mobile from "@/assets/Imagenes/Heros/Licenciaturas_gral_mobile.webp";
import ComoAprenderImg from "@/assets/Imagenes/Decorativos/ComoAprender.png";
import { Calendar, UserPlus, Phone } from "@lucide/astro";
import { Image } from "astro:assets";
import Niveles from "@/components/sections/acerca/Niveles.astro";
import Estadisticas from "@/components/sections/acerca/Estadisticas.astro";
import FAQEstadisticas from "@/components/sections/acerca/FAQEstadisticas.astro";
import CallAction from "@/components/sections/shared/CallAction.astro";
---

<Layout
  title="Licenciaturas UNE - 45 Carreras Universitarias"
  description="Licenciaturas en Universidad UNE con validez UDG y SEP. 45 carreras universitarias, modalidad presencial, convenios con 800+ empresas para prácticas."
>
  <main class="min-h-screen">
    <!-- Hero Section -->

    <section
      class="relative min-h-[85vh] md:min-h-[65vh] bg-cover bg-center bg-no-repeat"
    >
      {/* Desktop Background */}
      <div
        class="absolute inset-0 hidden md:block bg-cover bg-center bg-no-repeat"
        style={{ backgroundImage: `url(${Hero.src})` }}
      >
      </div>

      {/* Mobile Background */}
      <div
        class="absolute inset-0 block md:hidden bg-cover bg-center bg-no-repeat"
        style={{ backgroundImage: `url(${Hero_mobile.src})` }}
      >
      </div>

      {/* Hero Content */}
      <div
        class="relative z-10 container mx-auto px-4 h-full min-h-[85vh] md:min-h-[65vh] flex flex-col justify-center"
      >
        <div class="text-center text-white max-w-4xl mx-auto">
          {/* Main Title */}
          <h1 class="text-4xl md:text-7xl font-bold tracking-wider mb-4">
            Licenciaturas UNE
          </h1>
          <h2 class="text-2xl md:text-5xl font-bold tracking-wider mb-8">
            Especialízate en lo que te apasiona
          </h2>

          {/* Description */}
          <p
            class="text-lg md:text-xl leading-relaxed mb-16 max-w-3xl mx-auto font-light"
          >
            Tu carrera profesional merece la mejor formación. Descubre el
            programa que transformará tu conocimiento en liderazgo.
          </p>
        </div>
      </div>
    </section>

    <!-- Floating Card - ahora está fuera de la sección hero -->
    <div class="relative -top-16 z-20 px-4">
      <div class="bg-azul-une rounded-2xl px-8 shadow-2xl max-w-4xl mx-auto">
        <div
          class="flex flex-col sm:flex-row items-center justify-center gap-6"
        >
          <!-- CTA Buttons -->
          <div
            class="flex flex-col sm:flex-row gap-4 w-full justify-center text-white"
          >
            <!-- Agenda una Visita -->
            <a
              href="/contacto/solicitarvisita"
              class="hover:border-b hover:border-b-cyan-50 font-bold py-6 px-6 text-base transition-colors duration-300 shadow-lg flex flex-col items-center justify-center gap-2 whitespace-nowrap"
              aria-label="Ir a AGENDA UNA VISITA"
              title="AGENDA UNA VISITA"
            >
              AGENDA UNA VISITA
              <Calendar size={20} />
            </a>

            <!-- Iniciar Inscripción -->
            <a
              href="/contacto/formulario-inscripcion"
              class="hover:border-b hover:border-b-cyan-50 font-bold py-3 px-6 text-base transition-colors duration-300 shadow-lg flex flex-col items-center justify-center gap-2 whitespace-nowrap"
              aria-label="Ir a INICIAR INSCRIPCIÓN"
              title="INICIAR INSCRIPCIÓN"
            >
              INICIAR INSCRIPCIÓN
              <UserPlus size={20} />
            </a>

            <!-- Contactar -->
            <a
              href="/contacto/solicitarinformacion"
              class="hover:border-b hover:border-b-cyan-50 font-bold py-3 px-6 text-base transition-colors duration-300 shadow-lg flex flex-col items-center justify-center gap-2 whitespace-nowrap"
              aria-label="Ir a página de contacto"
              title="Contactar con nosotros"
            >
              CONTACTAR
              <Phone size={20} />
            </a>
          </div>
        </div>
      </div>
    </div>

    <section class="py-12 md:py-20 px-4">
      <div class="max-w-7xl mx-auto">
        <!-- Contenedor principal -->
        <div
          class="flex flex-col lg:flex-row-reverse items-center gap-8 lg:gap-16"
        >
          <!-- Imagen -->
          <div class="w-full lg:w-1/2">
            <div class="relative">
              <Image
                src={ComoAprenderImg}
                alt="Modelo educativo en línea UNE"
                class="w-full h-auto rounded-2xl"
                loading="lazy"
              />
            </div>
          </div>

          <!-- Contenido de texto -->
          <div class="w-full lg:w-1/2">
            <!-- Título -->
            <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6 md:mb-8">
              Conoce todo sobre nuestra plataforma
            </h2>

            <!-- Descripción -->
            <p class="text-base md:text-lg text-gray-700 leading-relaxed">
              Nuestra plataforma virtual está construida sobre un sistema
              académico robusto, ampliamente utilizado por instituciones
              educativas a nivel internacional. Ofrece un entorno intuitivo,
              seguro y flexible, donde podrás acceder a tus asignaturas,
              actividades, evaluaciones, recursos y retroalimentación docente en
              cualquier momento.
            </p>
          </div>
        </div>
      </div>
    </section>

    <Niveles />
    <Estadisticas />

    <FAQEstadisticas />
    <CallAction />
  </main>
</Layout>
