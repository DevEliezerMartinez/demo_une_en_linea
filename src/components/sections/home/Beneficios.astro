---
import { Image } from 'astro:assets';
import BeneficiosImg from '@/assets/Imagenes/Beneficios.png';

const beneficios = [
  {
    texto: "Cuentas con una plataforma que está habilitada las 24 horas, los 7 días de la semana."
  },
  {
    texto: "La evaluación es mediante actividades y foros, libre de exámenes."
  },
  {
    texto: "Es un sistema flexible que te permite estudiar a tu ritmo. Cursas una materia a la vez con una duración de 5 semanas."
  },
  {
    texto: "En nuestro modelo de excelencia académica no necesitas comprar libros, cuentas con acceso exclusivo a la Biblioteca Virtual UNE."
  }
];
---

<section class="py-12 md:py-20 px-4">
  <div class="max-w-7xl mx-auto">
    <!-- Título -->
    <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-12 md:mb-16">
      ¿Cuáles son los beneficios de estudiar en línea?
    </h2>

    <!-- Contenedor principal -->
    <div class="flex flex-col lg:flex-row items-center gap-8 lg:gap-16">
      <!-- Lista de beneficios -->
      <div class="w-full lg:w-1/2 space-y-6 md:space-y-8">
        {beneficios.map((beneficio, index) => (
          <div class="flex gap-4 items-start" key={index}>
            <!-- Punto rojo -->
            <div class="flex-shrink-0 mt-1">
              <div class="w-3 h-3 bg-rojo-une rounded-full"></div>
            </div>
            
            <!-- Texto del beneficio -->
            <p class="text-base md:text-lg text-gray-700 leading-relaxed">
              {beneficio.texto}
            </p>
          </div>
        ))}
      </div>

      <!-- Imagen -->
      <div class="w-full lg:w-1/2">
        <div class="relative">
          <Image 
            src={BeneficiosImg}
            alt="Estudiante trabajando en línea"
            class="w-full h-auto "
            loading="lazy"
          />
          
         
        </div>
      </div>
    </div>
  </div>
</section>